# leading comment
let x = 1;
# comment in statement position
	# space indented comment
	# tab indented comment
let y = 2;
# another comment
fn add(a, b) {
	# comment in function
	let sum = a + b;

	# return a + b
	sum
};

add(x, y)
